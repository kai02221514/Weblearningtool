# 推奨作業順序(recommended-work-order)

- 監査基準: `00-audit-scope.md` 参照 / 監査日: 2026-07-02 / 対象: `main` @ `16d2f93`
- ロードマップ(ai-research-development-roadmap.md)のPhaseとの対応: W1≈Phase 2.5直後の即時対応、W2≈Phase 3(仕様確定)、W3〜W4≈Phase 4(最小実装・検証基盤)、W5≈Phase 5(ルート生成)、W6≈Phase 6(UI・評価ログ接続)、W7≈Phase 7〜7.5(受入監査・評価開始ゲート)、W8≈Phase 5〜6と並行。**W2の各決定はPhase 3承認ゲート(OQ-004/005解消、01/02/09への反映、仕様基準SHA記録)を通過するまで実装に着手しない。**
- 優先順位の基準: この研究の目的(個別化ロジック・説明可能性・評価可能性)と後期評価の期限。UIの完成度や一般的ベストプラクティスは基準にしない。
- 原則: 小さく検証可能な変更を優先。mainへ直接コミットしない(ブランチ+PR)。各タスクは受入条件を持つ。
- 引き継ぎ注記: 本書はChatGPT/Codex等の別モデルが実行することを想定し、各タスクに根拠(RTM要件ID/リスクID)と依存を付す。**W2の各判断が終わるまでW5以降に着手しないこと。未確定事項を実装者が勝手に確定してはならない(10-handover禁止事項)。**
- 2026-07-03現在の補足: W1-1、W1-2、W1-3はPhase 2.5で完了済み。W1-4は原本確認待ち。Linear未接続のため、W2以降のIssue案は`docs/operations/linear-issue-backlog.md`に記録した。

## 依存関係の全体像

```
W1(即時・コード変更なし)──────────────────────────────┐
W2(研究者判断: OQ-004/005ほか)──┬─→ W5 routeGenerator ──┬─→ W7 評価準備
W3(テスト基盤)────────────────┤                        │
W4(整合性修正・ダミー排除)──────┘                        │
W2(OQ-009・同意)────────────→ W6 永続化・ログ・同意 ────┘
W8(教材・クイズのノード別化)= W2のOQ-006と範囲判断に依存(W5-W6と並行可)
```

## W1: 即時対応(今日〜数日。ほぼコード変更なし)

| # | タスク | 根拠 | 受入条件 |
|---|---|---|---|
| W1-1 | Supabaseダッシュボードで `kv_store_f3d88633` のRLS有効化を確認(無効なら有効化) | RSK-03 | RLS状態のスクリーンショット等を記録に残す |
| W1-2 | Dashboardの虚偽文言修正(「スコア算出」「最適な学習経路を提案」→「固定の推奨(開発中)」等の事実に即した表現)、「保存されます」文言の修正 | RSK-02 | 表示が実装の実態と一致する |
| W1-3 | 文書ステータスの宣言: docs/直下5文書とresearch系の正/参考を各文書冒頭に明記、decision-logに1件追記 | C-1, C-2, C-4, RSK-09 | 全文書に正/参考ラベルがある |
| W1-4 | 存在しない監査文書参照(11/12/14)の解消: 該当ファイルを追加するか参照を削除 | C-6 | research/00の参照が全て解決する |

## W2: 研究者判断(実装をブロックしている決定。指導教員との合意が必要なものを含む)

| # | 決定事項 | 対応OQ | ブロックしている作業 |
|---|---|---|---|
| W2-1 | 診断→開始ノード決定規則(項目セット・重み・レベル境界・スキップ規則)。年齢層・職業の除外の採否も記録(S-4) | OQ-004 | W5, W8 |
| W2-2 | ルート生成契約: 優先順位・同点処理・推薦件数・reasonCode/routeVersion等の出力型。**旧route-generation-spec.mdの案を項目別に採否判定するのが最短** | OQ-005 | W5, W6(ログ形式) |
| W2-3 | データ保存管理: 保存項目・仮名ID・保持期間・削除手順。旧evaluation-plan「保存すべきデータ」を検討材料に | OQ-009 | W6 |
| W2-4 | 確認テスト設計(問題数・合格閾値・再受験規則)と教材整備の範囲(全12ノードか評価経路のみか) | OQ-006 | W8 |
| W2-5 | 比較実験の採否と対象者(評価設計に影響。実装は単群でも進められるため優先度は上記より下) | OQ-002/003 | W7 |
| W2-6 | インプット形式選択(スライド/テキスト)の要件採否 | S-5/G-5 | W8 |

## W3: テスト・検証基盤(W2と並行可能。W5の前提)

| # | タスク | 根拠 | 受入条件 |
|---|---|---|---|
| W3-1 | `typescript` + `tsconfig.json`(strict)+ `vitest` を導入し、scripts に `typecheck` / `test` を追加。`"name"` のnpm規則違反を修正、package-lock.jsonをコミット対象に、`"*"`依存3件を固定 | RSK-06, RSK-10 | `npm ci && npm run typecheck && npm run test` がローカルで通る(型エラーは既存分を記録の上、段階的に解消可) |
| W3-2 | 既存不変条件のテスト化: ノードID形式、前提DAG閉包・無循環、errorMappings参照整合、mvpScopeガード | R-04,05,07,13,14 | notes/03の機械検証と同等の検証がvitestで再現される(現状ではerrorMappings参照整合テストは15件failするはず。W4-1とセットで green にする) |
| W3-3 | 最小CI(GitHub Actions: `npm ci && typecheck && test && build`)+ mainブランチ保護 | RSK-06, プロジェクト原則 | PRでCIが走り、main直コミットが防止される |

## W4: 決定済み仕様に対する整合性修正(W2を待たずに実施可能。各1PR規模)

| # | タスク | 根拠 | 受入条件 |
|---|---|---|---|
| W4-1 | errorMappingsの参照切れ15件の解消: MVP外6エラーに正規IDの対応先を与えるか、`mvpStatus: 'out-of-scope'` 等で明示し参照整合テストの対象規則を確定 | RSK-07, R-14 | W3-2の整合テストがgreen |
| W4-2 | ダミーデータ排除: 初期progressのモック実績(html-000完了・streak3・8時間・quizScores)を空に。quickTestResult固定値の除去(理解度テスト未実装なら項目自体を非表示) | RSK-11, M-2, M-9 | 新規ユーザーの初期状態が仕様上の初期状態(全て未完了・実績0)と一致 |
| W4-3 | デッドコード削除: Onboarding.tsx、questionConfig.json、未使用import(saveProfileはW6で接続するため対象外の判断も可) | RSK-13, RSK-14, R-32 | ビルド・テスト通過 |
| W4-4 | node.category/difficulty参照バグ修正(存在しないフィールドの表示を削除) | RSK-15 | 誤表示「上級」が消える |
| W4-5 | ドメインロジックのdomain層への抽出(挙動変更なし): determineLevel+スコア計算→`src/domain/levelAssessment.ts`、前提チェック+ノード状態分類→`src/domain/routing.ts`、analyzeCode→`src/domain/errorAnalysis.ts`(エラーID直結の戻り値にし、メッセージ文字列include判定を廃止) | R-14脆さ, notes/05 §9 | 既存挙動を保ったまま各関数に正常系・境界値・異常系のユニットテストが付く |

## W5: routeGenerator実装(W2-1/W2-2とW3の完了後。研究の中核)

| # | タスク | 根拠 | 受入条件 |
|---|---|---|---|
| W5-1 | `src/domain/routeGenerator.ts` を純関数で新規実装(入力: 診断結果・完了集合・テスト結果・エラー履歴・振り返り。出力: W2-2で確定した契約型。理由コードは判断過程から構造化) | R-10,11,15,30 | 同一入力→同一出力の決定性テスト、優先順位・同点・前提補完・フォールバック(入力不足/全完了/未知ID)のテストが通る |
| W5-2 | App/Dashboardへの接続: 固定`['html-010']`を廃止し、診断結果→開始ノード(W2-1規則)、学習結果→ルート更新(再生成トリガはW2-2で定義)を配線。Dashboardの推奨理由を構造化データから表示 | R-08,12 | 診断入力を変えると開始ノードが規則通り変わることをテストで確認。UI文言が実態と一致 |

## W6: 永続化・ログ・同意(W2-3の完了後。評価実施の必須前提)

| # | タスク | 根拠 | 受入条件 |
|---|---|---|---|
| W6-1 | 同意フロー(説明・同意記録・撤回導線)実装 | RSK-04, R-26 | 同意なしでは学習データが記録されない |
| W6-2 | セッション/トークン修正(signup直後のトークン取得、リロード復元) | RSK-12 | リロード後も認証・進捗が維持される |
| W6-3 | 進捗・イベントログの保存/復元API(サーバ受け口+フロント接続。saveProfile接続を含む)。仮名ID・版情報(routeVersion/catalogVersion)をW2-2/W2-3に従い付与 | RSK-01, R-17,18,20,21 | 学習操作→リロード→復元、および分析可能形式でのエクスポートが確認できる |

## W7: 評価準備(W2-5、W5、W6の完了後)

- 評価アンケート(research/05の中核観点、W2-5の設計に従う)の実装、パイロット1名での通し確認、データエクスポート検証。受入条件: 通し実行で評価計画が要求する全データが取得・エクスポートできる。

## W8: 教材・確認テストのノード別化(W2-4の範囲判断後。W5/W6と並行可)

- learningNodes/新データファイルに教材本文・quiz-{nodeId}問題を持たせるスキーマ追加 → 判断された範囲のコンテンツ作成 → LearningModule/QuizをcurrentNodeId駆動に変更。受入条件: 範囲内の各ノードで固有の教材・テストが表示され、誤答がnodeIdに紐付いて記録される。

## 対象外(このMVPでは実施しない。risk-register「対象外候補」参照)

依存関係の全面削減、Next.js移行、Completionの修了証・シェア機能、URLルーティング導入、網羅的デザイン改善。

## 見積り上の注意

- W1〜W4は小粒(各PR半日以内目安)で、W2の判断と完全に並行できる。
- 最長のリードタイムは「W2の研究者判断」自体である。実装ではなく意思決定がクリティカルパスにある点が本監査の主要な結論。
- 教材作成(W8)は判断された範囲次第で工数が大きく変動するため、W2-4を早期に決めることが後期スケジュールの安全余裕になる。
