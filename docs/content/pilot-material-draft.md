# 予備試行用教材本文草案（html-010 / html-021 / css-011）

## 0. メタ情報

- 状態: [研究者レビュー済み・予備試行前] 本書は予備試行用確認テスト試作（`docs/content/pilot-quiz-prototype.md`）の作問を進めるための仮教材案として、Claudeが独自に作成した追加提案である。研究者本人が予備試行用教材案として内容を確認し、3ノード9問の参照教材として使用可能と判断した。ただし、正本教材ではなく、本実験用教材として最終確定したものでもない。指導教員確認、UI組み込み、予備試行は未実施である。
- 対象Issue: Linear `KAI-15`
- 対象ノード: `html-010`、`html-021`、`css-011`
- 作成日: 2026-07-04
- 参照コミット（作成開始時点の`main`）: `9a46c2557046f70b279038650ba4ffd12454dd54`
- 教材版（案）: `material-html-010/v0.1`、`material-html-021/v0.1`、`material-css-011/v0.1`

## 1. 作成理由

2026-07-04時点のリポジトリを確認した結果、対象3ノードのノード別教材本文は存在しない。

- [確認済み事実] `src/data/learningNodes.ts`はノードのid、title、summary、prerequisites等のメタ情報のみを持ち、教材本文を持たない。
- [確認済み事実] `src/components/LearningModule.tsx`の教材コンテンツ（`learningContent`）は「HTMLの基礎」固定の汎用内容であり、`currentNodeId`に依存しない。
- [確認済み事実] Linear `KAI-15`のIssue本文に「現行Quizと教材はノード非依存のモックである」と記載されている。
- [確認済み事実] `docs/research/06-implementation-status.md`は「ノード対応済み確認テストデータ `quiz-{nodeId}`」を未実装としている。

問題仕様の作問原則（教材範囲との整合）を満たすには、問題が参照できる教材本文が必要である。ノード別教材本文の不在は、本来は「教材不足」として停止・報告すべき条件であった。そのため本書は、問題仕様の検討を進めるための仮教材案としてClaudeが独自に作成した追加提案として扱う。この作成経緯は保持する。研究者本人は本書の内容を確認し、予備試行用の3ノード9問が参照する教材案として使用可能と判断した。9問はすべて本書の節を参照基準として参照する。

[注意] 本書は正本教材ではなく、本実験用教材として最終確定したものでもない。予備試行結果に応じて修正可能である。教材内容が修正された場合、対応する9問の教材整合を再確認しなければならない。

## 2. 教材範囲の根拠

各ノードの範囲は次の正本資料に基づく。

| ノード | ノード定義（`src/data/learningNodes.ts`） | 到達目標（`docs/architecture/quiz-assessment.md` §9） |
| --- | --- | --- |
| `html-010` | HTML基本骨格(doctype / html / head / body)。最小構成のHTML文書を自力で作れる。前提: `html-000` | `<!DOCTYPE html>`, `html`, `head`, `body`の役割と配置を判別できる。 |
| `html-021` | 入れ子構造(DOMツリーの基礎)。親子・兄弟関係を意識してマークアップできる。前提: `html-020` | 親子関係と正しい閉じ順を判別できる。 |
| `css-011` | CSS基本構文(セレクタ / プロパティ / 値)。基本形と; {} の意味を理解して書ける。前提: `css-010` | セレクタ、プロパティ、値、波括弧、セミコロンの役割を判別できる。 |

補足:

- `docs/research/03-mvp-scope.md`「CSS色指定」の確定事項（D-003）により、色指定は`css-011`の教材内で扱う。本書§5.4で最小限の色名指定を扱う。
- DOCTYPEの詳細（標準モード/互換モード）は`html-011`（MVP対象外）の内容であるため、`html-010`では「文書の先頭に書く宣言」という位置付けのみを扱う。
- meta要素、charset、viewport、script読み込みは`html-012`/`html-013`（MVP対象外）の内容であるため扱わない。

## 3. html-010: HTML基本骨格

### 3.1 最小のHTML文書

Webページの元になるHTMLファイルには、共通の骨格がある。次のコードは、この単元で覚える最小のHTML文書である。

```html
<!DOCTYPE html>
<html>
  <head>
    <title>ページのタイトル</title>
  </head>
  <body>
    <p>ここに表示したい内容を書きます。</p>
  </body>
</html>
```

この骨格は次の順序と包含関係で構成される。

1. 最初の行に `<!DOCTYPE html>` を書く。
2. その下に `<html>` 〜 `</html>` を書き、文書全体を包む。
3. `<html>` の中に、`<head>` 〜 `</head>` と `<body>` 〜 `</body>` をこの順で書く。

### 3.2 <!DOCTYPE html> の役割

`<!DOCTYPE html>` は「この文書はHTMLです」とブラウザに伝える宣言である。

- 文書のいちばん先頭に、1回だけ書く。
- `<html>` などの要素の外側（前）に書く。要素の中には書かない。
- これは要素（タグのペア）ではなく宣言なので、終了タグはない。

### 3.3 html要素の役割

`<html>` 〜 `</html>` は文書全体を包む要素である。`<!DOCTYPE html>` を除いて、HTML文書のすべての内容は `<html>` の中に入る。`<html>` の直下には `<head>` と `<body>` の2つが入る。

### 3.4 head要素の役割

`<head>` 〜 `</head>` には、ページ本文としては表示されない「ページに関する情報」を書く。

- この単元では、ページのタイトルを表す `<title>` を書く場所として覚える。
- `<title>` に書いた文字は、ページの本文ではなく、ブラウザのタブなどに表示される。

### 3.5 body要素の役割

`<body>` 〜 `</body>` には、ブラウザの画面に表示される内容を書く。段落 `<p>` などの表示したい要素はすべて `<body>` の中に書く。

### 3.6 よくある間違い

- `<head>` と `<body>` の順序を逆にする。正しくは `<head>` が先、`<body>` が後である。
- 表示したい文章を `<head>` の中に書いてしまう。表示する内容は `<body>` に書く。
- `<!DOCTYPE html>` を `<html>` の中に書いてしまう。宣言は文書の先頭、`<html>` の外側に書く。

## 4. html-021: 入れ子構造

前提ノード `html-020` で学んだこと（この単元で使う知識）: 要素は原則として開始タグ、内容、終了タグの3つで構成される。例: `<p>こんにちは</p>`。

### 4.1 要素の中に要素を入れる（入れ子）

要素の内容には、文字だけでなく別の要素を入れることができる。これを入れ子（ネスト）と呼ぶ。

```html
<p><strong>重要</strong>なお知らせです。</p>
```

- 外側の要素を親要素と呼ぶ。上の例では `<p>` が親要素である。
- 内側の要素を子要素と呼ぶ。上の例では `<strong>` が `<p>` の子要素である。
- 親要素とは、その要素を直接包んでいる1つ外側の要素を指す。上の例を `<body>` の中に書いた場合でも、`<strong>` の親要素は `<p>` であり、`<body>` は親のさらに外側（祖先）である。

### 4.2 閉じる順序の規則

入れ子にした要素は、内側の要素から先に閉じる。後から開始したタグを先に閉じる、と覚えてもよい。

```html
<!-- 正しい: strongを閉じてからpを閉じる -->
<p><strong>重要</strong>なお知らせです。</p>
```

### 4.3 交差した入れ子は誤り

閉じる順序を間違えると、タグが交差してしまう。次の例は誤りである。

```html
<!-- 誤り: pの中で開始したstrongを、pを閉じた後に閉じている -->
<p><strong>重要</p></strong>
```

タグの対応関係が交差しているHTMLは、正しい入れ子構造ではない。ブラウザ上で一見表示されることがあっても、正しい文書構造として扱われない。

### 4.4 インデントで構造を見やすくする

親子関係が深くなるときは、子要素を字下げ（インデント）して書くと、どの要素がどの要素の中にあるかが読みやすくなる。

```html
<body>
  <p>
    <strong>重要</strong>なお知らせです。
  </p>
</body>
```

### 4.5 よくある間違い

- 外側の要素を先に閉じてしまい、タグが交差する。
- 内側の要素の終了タグを書き忘れる（`html-020`の閉じタグの復習も参照）。
- 親要素と、さらに外側の要素（祖先）を区別できない。

## 5. css-011: CSS基本構文

前提ノード `css-010` で学んだこと（この単元で使う知識）: CSSはHTMLに適用して見た目を変えるための言語であり、外部CSS・内部CSS・インラインの適用方法がある。この単元では、CSSそのものの書き方を学ぶ。

### 5.1 CSSルールセットの基本形

CSSは次の形のまとまり（ルールセット）で書く。

```css
p {
  color: red;
}
```

各部分の名前と役割は次のとおりである。

| 部分 | 上の例 | 役割 |
| --- | --- | --- |
| セレクタ | `p` | どのHTML要素に適用するかを指定する |
| 波括弧 `{ }` | `{ ... }` | そのセレクタに適用する宣言のまとまりを囲む |
| プロパティ | `color` | 何を変えるか（この例では文字の色）を指定する |
| コロン `:` | `:` | プロパティと値を区切る |
| 値 | `red` | どう変えるか（この例では赤に）を指定する |
| セミコロン `;` | `;` | 1つの宣言の終わりを示す |

「プロパティ: 値;」のひとまとまりを宣言と呼ぶ。

### 5.2 複数の宣言を書く

1つのルールセットの波括弧の中には、宣言を複数書ける。各宣言の終わりには必ずセミコロンを書く。

```css
p {
  color: red;
  font-size: 20px;
}
```

[補足] 最後の宣言のセミコロンを省略できる場合もあるが、この教材では書き忘れを防ぐため、すべての宣言の終わりにセミコロンを付けると覚える。

### 5.3 記号の書き間違いに注意

- プロパティと値の区切りはコロン `:` である。等号 `=` は使わない。
- 宣言のまとまりを囲むのは波括弧 `{ }` である。丸括弧 `( )` や角括弧 `[ ]` は使わない。
- セミコロン `;` を忘れると、その後の宣言が正しく適用されないことがある。

### 5.4 色の指定（この単元で扱う最小範囲）

文字の色は `color` プロパティで変える。この単元では、値として `red`、`blue`、`green` のような基本的な色名を使う。

```css
p {
  color: blue;
}
```

（16進カラーコードなどの詳細な色指定はこの単元では扱わない。）

### 5.5 この単元で使うプロパティ

この単元の説明と練習では、次の2つだけを使う。

- `color`: 文字の色を変える
- `font-size`: 文字の大きさを変える（例: `20px`）

### 5.6 よくある間違い

- コロンと等号の混同（`color= red;` は誤り）。
- 波括弧と丸括弧の混同（`p ( ... )` は誤り）。
- セミコロンの書き忘れ。
- セレクタを波括弧の中に書いてしまう。

## 6. 未確定事項

- 本書は研究者レビュー済み・予備試行前の教材案であり、正本教材ではない。
- 本実験用教材としての最終確定、指導教員確認、予備試行、UI組み込みは未実施である。
- 予備試行結果に応じて教材本文を修正する可能性がある。
- 教材本文を修正した場合、3ノード9問との整合を再確認する。
- UI（`LearningModule.tsx`）への組み込み方法、表示形式、スライド分割はKAI-15の実装作業で扱う。本書では決定しない。
- 教材版番号の運用（`material-{nodeId}/v{major}.{minor}`という形式の採用可否）は研究者確認が必要である。
- `html-020`（html-021の前提）と`css-010`（css-011の前提）の教材本文も未整備である。本書の§4冒頭・§5冒頭の「前提ノードで学んだこと」は、当該前提ノードの教材が整備された際に整合を再確認する必要がある。
