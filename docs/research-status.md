> 状態: 参考資料・旧文書
> 本文書は過去の検討内容を保持するための資料であり、現在の研究仕様の正本ではない。
> 矛盾する場合は`docs/research/`配下および最新の`docs/research/09-decision-log.md`を優先する。

## ChatGPT / Codex 同期ルール

- 研究方針の一次整理はChatGPTプロジェクトで行う

- 実装状況の一次情報源はGitHubリポジトリとする

- Codexで実装した内容は、必ず `docs/` 配下の該当ファイルに反映する

- MVP範囲やID体系を変更した場合は `docs/mvp-scope.md` を更新する

- ルート生成ロジックを変更した場合は `docs/route-generation-spec.md` を更新する

- 評価方法を変更した場合は `docs/evaluation-plan.md` を更新する

- ChatGPTで研究相談を行う際は、必要に応じてGitHub上の `docs/` を最新前提として扱う

# 研究・実装ステータス

- 最終更新日: 2026-06-19
- 文書ステータス: MVPスコープ・正規ID確定済み

## 研究テーマ

初学者向けHTML/CSS学習支援において、事前診断、学習進捗、確認テスト、実践課題で検出したエラー、学習後の振り返りを統合し、学習者ごとの学習ルートを生成・更新する仕組みを研究する。

この表現は現行実装から整理した暫定テーマであり、正式な研究題目は未確定とする。

## 研究目的

- 学習者の知識・技能・つまずきに応じて、次に学ぶべきHTML/CSSノードを説明可能な形で提示する。
- 固定順序の教材と比べ、理解度、エラーからの回復、学習効率、継続しやすさが改善するかを評価する。
- ルートを生成した根拠を保存し、研究結果を再現・検証できる状態にする。

## 現在の実装状況の概略

React/Vite製のプロトタイプとして、認証、初期アンケート、チュートリアル、ダッシュボード、教材、確認テスト、実践課題、振り返り、修了画面までの画面遷移が用意されている。

データ面では、[学習ノード定義](../src/data/learningNodes.ts)にHTML 32件、CSS 31件のノードと前提関係があり、[エラーマッピング定義](../src/data/errorMappings.ts)に14件のエラー分類がある。[MVPスコープ](./mvp-scope.md)では、そのうち12ノードと8エラーを初回MVP対象として確定した。MVP対象の前提関係、エラー参照、初期推奨値、進捗IDは正規ノードIDへ揃えたが、確認テストのID・ノード対応とルート生成は未実装である。永続化は認証とプロファイル保存の一部に限られ、学習履歴はアプリのメモリ上にのみ保持される。

## 2026-06-19の確定事項

- MVP対象をHTML 7ノード、CSS 5ノードの計12ノードに限定した。
- 正規ノードIDを `^(html|css)-[0-9]{3}$` とした。
- `html-basics` 等の画面用ID、英語slug、ドット形式IDを正規ノードIDとして扱わない。
- MVP前提関係を12ノード内で閉じた。
- MVP対象エラーを8件に限定し、全参照先をMVPノードへ揃えた。
- 色指定は独立ノードにせず、`css-011` の教材内で扱うことにした。
- 確認テストIDを `quiz-{nodeId}`、中心実践課題IDを `practice-profile-card` とした。
- 現行候補表は「25件」と記載されていたが実数は26件だったため、12件採用、14件除外として訂正した。

## 機能別分類

### 実装済み

- 認証、初期アンケート、チュートリアル、学習、確認テスト、実践、振り返り、修了の画面コンポーネント
- HTML/CSS学習ノード候補と各ノードの前提関係の静的データ
- `MVP_NODE_IDS` と `getMvpLearningNodes()` によるMVP 12ノードの単一定義
- Skill / Rule / Knowledgeの区分を持つエラー分類データ
- 初期アンケートの回答スコアリングと初級・中級・上級の判定
- 固定問題による確認テストとスコア算出
- 実践課題に対する簡易的な文字列・正規表現ベースのエラー検出
- `practice-profile-card` と対象10ノードの課題メタデータ
- 振り返り入力と、選択されたつまずきに応じた簡易推奨文の生成
- Supabaseを利用したサインアップ、サインイン、プロファイル保存用エンドポイント

### 部分実装

- 推奨ノード表示: 初期値と前提判定は正規ノードIDを使うが、診断・テスト・エラー・振り返りを統合した生成結果ではない。
- ルート生成: 前提条件を満たす候補の抽出処理はあるが、順序付き学習ルート、理由コード、決定的な優先順位は未実装である。
- エラーからの推薦: MVP 8エラーは正規IDと `nodeRefs` が一致した。MVP外6エラーの旧ドット形式参照は後続移行対象である。
- 進捗管理: React state上の完了識別子を `completedNodeIds` に統一したが、初期値はデモ用で、再読み込み後には復元されない。
- 診断: 事前アンケートとレベル判定はあるが、回答を学習ノードやルート開始位置へ変換する規則はない。
- プロファイル保存: APIは存在するが、サインアップ直後のアンケートではアクセストークンがなく、実際の保存処理が呼ばれていない。

### 未実装

- `quiz-{nodeId}` に基づく確認テストID、問題ID、対応ノードIDの実装
- 診断・進捗・テスト・エラー・振り返りを統合するルート生成サービス
- ルート生成理由、ルート版、データ版を含む出力仕様
- 学習進捗、テスト試行、エラー履歴、振り返り、生成ルートの永続化と復元
- 研究評価用の事前・事後アンケート、イベント計測、データ出力
- 固定ルート等との比較条件、評価指標、対象者、実施手順の確定
- ID参照整合性、前提関係の循環、ルート順序を検証する自動テスト

## 現時点の最優先事項

最優先だった[MVPスコープ](./mvp-scope.md)と正規ID体系の確定は完了した。次は、この確定内容を[ルート生成仕様](./route-generation-spec.md)の具体的な入出力契約へ反映し、MVP集合だけを扱うルート生成ロジックを実装する。

優先順は次のとおり。

1. `route-generation-spec.md` をMVP 12ノード、8エラー、確認テスト・実践課題IDの契約へ更新する。
2. 正規IDを入力・出力に使う `routeGenerator` と判定規則を実装する。
3. ID整合性、前提補完、循環、決定性を自動テストする。
4. 研究評価に必要な履歴を永続化する。
5. 評価アンケートとデモ導線を整備する。

## ChatGPT/Codex同期ルール

- `docs/` を研究方針と実装契約の共有元とする。チャット内だけで確定事項を保持しない。
- ChatGPTで研究上の判断を確定した場合は、対象文書の「未確定事項」を更新し、決定内容・決定日・理由を記録する。
- Codexが実装を変更する場合は、同じ変更単位で `research-status.md`、該当仕様書、`implementation-plan.md` の状態も更新する。
- 「実装済み」は、コードが存在するだけでなく、正規データで動作し、完了条件に対応する検証が通った状態を指す。
- 暫定案は「案」または「未確定」と明記し、実装上の既定値と研究上の決定を混同しない。
- IDや保存形式を変更する場合は、変更前後の対応、データ版、移行要否を文書へ残す。
- 実装開始時は [実装計画](./implementation-plan.md) と [ルート生成仕様](./route-generation-spec.md) を確認し、完了時は実コードとの差分がないか見直す。
- 評価方法に影響する変更は、実装だけで完結させず [評価計画](./evaluation-plan.md) も更新する。
